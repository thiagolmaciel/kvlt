# KVLT

Portal privado do Jaguara.

Só entra quem já sabe o que é isto.

---

```bash
npm install
npm run dev
```

Copie `.env.local.example` para `.env.local` e preencha `ANTHROPIC_API_KEY` para acordar o Jaguara.

---

*Se você está lendo isto e não é Th. nem Rd., feche a aba.*
